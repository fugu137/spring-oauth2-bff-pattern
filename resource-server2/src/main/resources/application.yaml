logging:
  level:
    web: TRACE
    org.springframework.security: TRACE

server:
  port: 9001

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-server:8000